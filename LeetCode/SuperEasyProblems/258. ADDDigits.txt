class Solution {
public:
    int addDigits(int num) {
        while (num>9){
            string digit=to_string(num);
            int size=digit.length();
            num=0;
            for (int i=0;i<size;i++){
                num=(digit[i]-'0')+num;
            }
        }
        return num;
    }
};