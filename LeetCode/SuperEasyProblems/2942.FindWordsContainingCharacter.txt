class Solution(object):
    def findWordsContaining(self, words, x):
        array=[]
        for i in range(len(words)):
          if words[i].find(x)!=-1 :
            array.append(i)
        return array