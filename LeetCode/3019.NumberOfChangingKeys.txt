class Solution(object):
    def countKeyChanges(self, s):
       s=s.lower()
       key=0
       for i in range(len(s)):
        if i == (len(s)-1):
            return key
        if s[i]!=s[i+1]:
            key=key+1

